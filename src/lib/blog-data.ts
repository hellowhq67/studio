
import type { BlogPost } from './types';

// This file is no longer used for static data, but we can keep the type definition.
// The blog is now generated dynamically by AI.
export const blogPosts: BlogPost[] = [];
